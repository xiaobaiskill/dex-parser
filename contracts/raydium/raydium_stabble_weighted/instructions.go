// Code generated by https://github.com/gagliardetto/anchor-go. DO NOT EDIT.
// This file contains instructions.

package weighted_swap

import (
	"bytes"
	"fmt"
	errors "github.com/gagliardetto/anchor-go/errors"
	binary "github.com/gagliardetto/binary"
	solanago "github.com/gagliardetto/solana-go"
)

// Builds a "accept_owner" instruction.
func NewAcceptOwnerInstruction(
	pendingOwnerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "pending_owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(pendingOwnerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "change_max_supply" instruction.
func NewChangeMaxSupplyInstruction(
	// Params:
	newMaxSupplyParam uint64,

	// Accounts:
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ChangeMaxSupply[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `newMaxSupplyParam`:
		err = enc__.Encode(newMaxSupplyParam)
		if err != nil {
			return nil, errors.NewField("newMaxSupplyParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "change_swap_fee" instruction.
func NewChangeSwapFeeInstruction(
	// Params:
	newSwapFeeParam uint64,

	// Accounts:
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ChangeSwapFee[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `newSwapFeeParam`:
		err = enc__.Encode(newSwapFeeParam)
		if err != nil {
			return nil, errors.NewField("newSwapFeeParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "deposit" instruction.
// add liquidity
func NewDepositInstruction(
	// Params:
	amountsParam []uint64,
	minimumAmountOutParam uint64,

	// Accounts:
	userAccount solanago.PublicKey,
	userPoolTokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	poolAuthorityAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
	vaultAuthorityAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	tokenProgram2022account solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Deposit[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `amountsParam`:
		err = enc__.Encode(amountsParam)
		if err != nil {
			return nil, errors.NewField("amountsParam", err)
		}
		// Serialize `minimumAmountOutParam`:
		err = enc__.Encode(minimumAmountOutParam)
		if err != nil {
			return nil, errors.NewField("minimumAmountOutParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "user": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, true))
		// Account 1 "user_pool_token": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userPoolTokenAccount, true, false))
		// Account 2 "mint": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 4 "pool_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAuthorityAccount, false, false))
		// Account 5 "vault": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
		// Account 6 "vault_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAuthorityAccount, false, false))
		// Account 7 "token_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 8 "token_program_2022": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgram2022account, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "initialize" instruction.
// initialize a pool
func NewInitializeInstruction(
	// Params:
	swapFeeParam uint64,
	weightsParam []uint64,
	maxCapsParam []uint64,

	// Accounts:
	ownerAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	poolAuthorityAccount solanago.PublicKey,
	withdrawAuthorityAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Initialize[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `swapFeeParam`:
		err = enc__.Encode(swapFeeParam)
		if err != nil {
			return nil, errors.NewField("swapFeeParam", err)
		}
		// Serialize `weightsParam`:
		err = enc__.Encode(weightsParam)
		if err != nil {
			return nil, errors.NewField("weightsParam", err)
		}
		// Serialize `maxCapsParam`:
		err = enc__.Encode(maxCapsParam)
		if err != nil {
			return nil, errors.NewField("maxCapsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "mint": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 2 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 3 "pool_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAuthorityAccount, false, false))
		// Account 4 "withdraw_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(withdrawAuthorityAccount, false, false))
		// Account 5 "vault": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "pause" instruction.
func NewPauseInstruction(
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "reject_owner" instruction.
func NewRejectOwnerInstruction(
	pendingOwnerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "pending_owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(pendingOwnerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "shutdown" instruction.
// shutdown the zero-liquidity pool
func NewShutdownInstruction(
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, true, false))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "swap" instruction.
// swap
func NewSwapInstruction(
	// Params:
	amountInParam *uint64,
	minimumAmountOutParam uint64,

	// Accounts:
	userAccount solanago.PublicKey,
	userTokenInAccount solanago.PublicKey,
	userTokenOutAccount solanago.PublicKey,
	vaultTokenInAccount solanago.PublicKey,
	vaultTokenOutAccount solanago.PublicKey,
	beneficiaryTokenOutAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	withdrawAuthorityAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
	vaultAuthorityAccount solanago.PublicKey,
	vaultProgramAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Swap[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `amountInParam` (optional):
		{
			if amountInParam == nil {
				err = enc__.WriteOption(false)
				if err != nil {
					return nil, errors.NewOption("amountInParam", fmt.Errorf("error while encoding optionality: %w", err))
				}
			} else {
				err = enc__.WriteOption(true)
				if err != nil {
					return nil, errors.NewOption("amountInParam", fmt.Errorf("error while encoding optionality: %w", err))
				}
				err = enc__.Encode(amountInParam)
				if err != nil {
					return nil, errors.NewField("amountInParam", err)
				}
			}
		}
		// Serialize `minimumAmountOutParam`:
		err = enc__.Encode(minimumAmountOutParam)
		if err != nil {
			return nil, errors.NewField("minimumAmountOutParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "user": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, true))
		// Account 1 "user_token_in": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userTokenInAccount, true, false))
		// Account 2 "user_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userTokenOutAccount, true, false))
		// Account 3 "vault_token_in": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultTokenInAccount, true, false))
		// Account 4 "vault_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultTokenOutAccount, true, false))
		// Account 5 "beneficiary_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(beneficiaryTokenOutAccount, true, false))
		// Account 6 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 7 "withdraw_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(withdrawAuthorityAccount, false, false))
		// Account 8 "vault": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
		// Account 9 "vault_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAuthorityAccount, false, false))
		// Account 10 "vault_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultProgramAccount, false, false))
		// Account 11 "token_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "swap_v2" instruction.
func NewSwapV2Instruction(
	// Params:
	amountInParam *uint64,
	minimumAmountOutParam uint64,

	// Accounts:
	userAccount solanago.PublicKey,
	mintInAccount solanago.PublicKey,
	mintOutAccount solanago.PublicKey,
	userTokenInAccount solanago.PublicKey,
	userTokenOutAccount solanago.PublicKey,
	vaultTokenInAccount solanago.PublicKey,
	vaultTokenOutAccount solanago.PublicKey,
	beneficiaryTokenOutAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	withdrawAuthorityAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
	vaultAuthorityAccount solanago.PublicKey,
	vaultProgramAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	token2022ProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SwapV2[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `amountInParam` (optional):
		{
			if amountInParam == nil {
				err = enc__.WriteOption(false)
				if err != nil {
					return nil, errors.NewOption("amountInParam", fmt.Errorf("error while encoding optionality: %w", err))
				}
			} else {
				err = enc__.WriteOption(true)
				if err != nil {
					return nil, errors.NewOption("amountInParam", fmt.Errorf("error while encoding optionality: %w", err))
				}
				err = enc__.Encode(amountInParam)
				if err != nil {
					return nil, errors.NewField("amountInParam", err)
				}
			}
		}
		// Serialize `minimumAmountOutParam`:
		err = enc__.Encode(minimumAmountOutParam)
		if err != nil {
			return nil, errors.NewField("minimumAmountOutParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "user": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, true))
		// Account 1 "mint_in": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintInAccount, false, false))
		// Account 2 "mint_out": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintOutAccount, false, false))
		// Account 3 "user_token_in": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userTokenInAccount, true, false))
		// Account 4 "user_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userTokenOutAccount, true, false))
		// Account 5 "vault_token_in": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultTokenInAccount, true, false))
		// Account 6 "vault_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultTokenOutAccount, true, false))
		// Account 7 "beneficiary_token_out": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(beneficiaryTokenOutAccount, true, false))
		// Account 8 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 9 "withdraw_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(withdrawAuthorityAccount, false, false))
		// Account 10 "vault": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
		// Account 11 "vault_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAuthorityAccount, false, false))
		// Account 12 "vault_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultProgramAccount, false, false))
		// Account 13 "token_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 14 "token_2022_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(token2022ProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "transfer_owner" instruction.
func NewTransferOwnerInstruction(
	// Params:
	newOwnerParam solanago.PublicKey,

	// Accounts:
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_TransferOwner[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `newOwnerParam`:
		err = enc__.Encode(newOwnerParam)
		if err != nil {
			return nil, errors.NewField("newOwnerParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "unpause" instruction.
func NewUnpauseInstruction(
	ownerAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "owner": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 1 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "withdraw" instruction.
// remove liquidity
func NewWithdrawInstruction(
	// Params:
	amountParam uint64,
	minimumAmountsOutParam []uint64,

	// Accounts:
	userAccount solanago.PublicKey,
	userPoolTokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	poolAccount solanago.PublicKey,
	withdrawAuthorityAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
	vaultAuthorityAccount solanago.PublicKey,
	vaultProgramAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	tokenProgram2022account solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Withdraw[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `amountParam`:
		err = enc__.Encode(amountParam)
		if err != nil {
			return nil, errors.NewField("amountParam", err)
		}
		// Serialize `minimumAmountsOutParam`:
		err = enc__.Encode(minimumAmountsOutParam)
		if err != nil {
			return nil, errors.NewField("minimumAmountsOutParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "user": Read-only, Signer, Required
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, true))
		// Account 1 "user_pool_token": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(userPoolTokenAccount, true, false))
		// Account 2 "mint": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "pool": Writable, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(poolAccount, true, false))
		// Account 4 "withdraw_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(withdrawAuthorityAccount, false, false))
		// Account 5 "vault": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
		// Account 6 "vault_authority": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultAuthorityAccount, false, false))
		// Account 7 "vault_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(vaultProgramAccount, false, false))
		// Account 8 "token_program": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 9 "token_program_2022": Read-only, Non-signer, Required
		accounts__.Append(solanago.NewAccountMeta(tokenProgram2022account, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}
